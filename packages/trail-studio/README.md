# @juicerq/trail-studio

Local readonly Studio for Trail SQLite event stores.

```sh
bunx @juicerq/trail-studio --db ./obs.db
```

By default the server binds to `127.0.0.1` and opens the browser. Pass `--no-open` to keep it in the terminal.
